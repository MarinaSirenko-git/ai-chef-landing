{
  "name": "ai-chef-landing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:cf": "wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^8.0.12",
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
